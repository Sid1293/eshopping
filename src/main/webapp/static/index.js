app.controller('indexCtrl', function($scope, $http, $window, $filter,$location, $timeout) {
	var baseUrl = $location.protocol() + "://" + location.host + u;
	
	$scope.firstName = "John";
     $scope.lastName = "Doe";
});